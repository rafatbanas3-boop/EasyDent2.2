using System.Drawing;

namespace EasyDent2
{
    // Renamed to avoid duplicate class name
    public static class ThemeColorsEnhanced
    {
        public static readonly Color Primary = Color.FromArgb(0, 120, 215);
        // ... other color members from the file you renamed
    }
}